# Package minimization policy v0.1

Rules:

- no SUF-root file without a routing, governance, citation, or package-control job
- no current package surface without explicit inclusion in the package current-surfaces registry
- preserve scholarly docs, but classify them instead of letting them blur into one undifferentiated surface
- prefer wrapper routing over duplicate rewrites
