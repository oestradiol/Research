# Changelog

## Unreleased

- added a first-pass official corpus inventory for the New Zealand demonstrated route
- added direct New Zealand event-page sources for border, alert-level, and transition milestones
- deepened the seed event ledger with direct `D-family E` and transition events
- added a first-pass readout showing one bounded route-level yield from the seed ledger
- added a first-pass dependency graph from the coded event set
- added a first minimal `I` summary from the seed graph
- added first bounded `C` and `L` passes from the seeded event chain
- deepened the seed ledger with April-May transition-planning and legal-handoff events
- coded direct implementation-side D-family `D` and `E` events from DPMC and Police material into the seed ledger
- added a first conjoint window comparison across Comparator A, the main perturbation interval, and Comparator B
- added a first bounded sensitivity and null note for the current New Zealand route readout
- deepened the bounded sensitivity/null note with unit-reassignment and bundled-event-splitting perturbations
- strengthened D-family `D` and `E` source assembly with direct DPMC implementation and Police guidance surfaces
- replaced an unstable Beehive patrol citation with a fixed-archived Police equivalent and upgraded recent DPMC/Police route archives to fixed captures
- added one more direct Police implementation-side event for the extended Level 4 interval and propagated the resulting ledger/readout updates
- updated route status surfaces so execution progress is visible without overstating completion
- added a route-local source discovery log with explicit acceptance and rejection reasons under the live-plus-fixed-archive admission rule
- added five direct DPMC implementation-side events and lifted the New Zealand seed ledger to `30` coded events with a `23`-event main interval
- added a first route-local estimator implementation for `I`, `C`, `L`, and dashboard-style `U`
- expanded the sensitivity/null note to a `10`-check robustness pass tied to the current `30`-event baseline
- added a public contribution/payoff candidate note with three provisional statements
- added a dedicated public IAF downstream-role clarification note
- updated package surfaces to document that the New Zealand gate for a first Taiwan comparative tranche is now cleared
- opened the first Taiwan comparative branch with a design note, archive-clean official corpus inventory, seed event ledger, and bounded NZ-Taiwan comparison note

## `v0.1.0` - 2026-04-01

Local research-phase snapshot for the public package in this tree. Local metadata, licensing, and citation scaffolding are already in place; remote hosting, hosted package URLs, remote-grounded changelog history, and first release packaging remain intentionally deferred.

### Included in this snapshot

- research-phase rescope of the public package
- applications split into demonstrated routes and a targeted research map
- research-ready New Zealand route package with design, corpus, and comparator docs
- populated standard and extended reading lists
- updated public entry, status, roadmap, and policy surfaces
