# First-Pass `I` Summary

## Purpose

This file is the first minimal integration summary for the New Zealand demonstrated route.

It is intentionally pre-final. It uses the current [Event Ledger Seed](event-ledger-seed.md) and [First-Pass Dependency Graph](first-pass-dependency-graph.md) to produce a cautious `I` readout without pretending the route already has a finished estimator stack.

## Rule for this pass

This `I` summary uses only simple public proxies:

- active directed-edge count
- occupied-edge ratio
- participation breadth
- cross-cluster routing share
- issuing concentration

These are route-support proxies, not final estimator choices.

## Current proxy snapshot

### 1. Active directed edges

- active directed edges observed: `24`
- possible directed edges among `7` `sigma1` units excluding self-links: `42`
- occupied-edge ratio: `24 / 42 = 0.571`

Interpretation:

The current seed does **not** produce an all-to-all mesh. It produces a selective but non-trivial directed network. That is consistent with a bounded institutional coordination system rather than a flat field of disconnected actors.

### 2. Participation breadth

- `sigma1` units present as issuer or receiver: `7 / 7`

Interpretation:

Every default `sigma1` unit is already active in the seed network. That matters because the route is not being held together by only one or two symbolic actors.

### 3. Cross-cluster routing

Using the current `sigma2` reading:

- strategic coordination: strategic executive coordination; public-service system coordination
- response operations: public-health policy and command; emergency-management coordination; border-control coordination; enforcement and compliance
- public alignment: public-information coordination

The current seed shows:

- cross-cluster unique directed edges: `16 / 24`
- cross-cluster weighted edge occurrences: `95 / 127 = 0.75`

Interpretation:

Most of the observed routing is cross-cluster rather than trapped inside one cluster. That is a strong first-pass sign of integration rather than merely parallel activity inside isolated subsystems.

### 4. Issuing concentration

- strategic executive coordination issues `22 / 35` coded events

Interpretation:

The current `I` shape is **hub-centred**, not egalitarian. That does not weaken the route. It clarifies what kind of integration is being observed: a strongly central issuing structure with broad cross-unit reach.

## First minimal `I` reading

The most defensible current reading is:

**The seed route already shows moderate-to-strong integration in the form of a central issuing hub with broad cross-cluster reach and full unit participation, rather than a flat or fragmented coordination pattern. The newly admitted Police continuity events, transition-planning material, and direct Level 2 preparation paper make that shape denser without changing its basic structure.**

That reading is still bounded:

- it does not claim final numerical scoring
- it does not claim all units are equally integrated
- it does not claim the same shape holds in every window
- it does not yet settle how much of this integration is durable versus crisis-compressed

## Why this matters

This is the first place where the framework starts doing more than saying "coordination happened."

The current `I` pass can already distinguish between at least three possibilities:

1. fragmented coordination with only symbolic centre claims
2. broad but flat undifferentiated activity
3. hub-centred, cross-cluster integration

The current seed supports the third reading best.

## Limits

This first pass still lacks:

- final edge weighting
- window-specific `I` comparison beyond simple descriptive counts
- missingness treatment
- sensitivity checks around unit reassignment
- any serious sensitivity treatment for the now-existing conjoint `I/C/L` comparison

## Next best step

The next build sequence should be:

1. keep the current route-local estimator implementation fixed unless new evidence forces a rule change
2. stress-test the conjoint `I/C/L` window comparison against the now-`35`-event ledger and the deeper Taiwan tranche
3. then deepen the ledger further only with newly admitted durable D-family `D` and `E` material

## Status

`first-pass I summary`
