# Tooling v0.2 Migration Debt — 2026-04-19

> **Status**: Documented. 7 remaining validation failures post-migration.
> **Trigger**: `package_doctor.py` run shows structural debt from v0.1→v0.2 governance migration.

## Completed Fixes

| File | Change | Status |
|------|--------|--------|
| `authoritative_guard.py` | Updated v0.1→v0.2 manifest reference | ✅ Fixed |
| `package_doctor.py` | Updated v0.1→v0.2 index reference | ✅ Fixed |
| `tools/.../governance.py` | Multiple v0.1→v0.2 migration updates | ✅ Fixed |
| `REGISTRY_MANIFEST_v0_2.json` | Added missing root Python files + allowlist | ✅ Fixed |

## Remaining Debt (7 failures)

### 1. File Registry Coverage
**Failure**: `v2-file-registry-coverage: Registry manifest does not match the actual file set.`

**Root cause**: `REGISTRY_MANIFEST_v0_2.json` catalog is stale relative to actual filesystem.

**Evidence**: 
```
validate_repository_file_registry() shows missing/extra files
Path: governance/REGISTRY_MANIFEST_v0_2.json
```

**Fix required**: Regenerate `REGISTRY_MANIFEST_v0_2.json` from actual file tree.

---

### 2. Subsystem Entry Surface Scope
**Failure**: `subsystem-entry-surfaces-in-scope: Some subsystem entry surfaces fall outside their declared scope.`

**Root cause**: `internal` subsystem declares entry `Internal/Framework Control.md` but scope_prefix is `../Internal/`. The entry path doesn't match the prefix pattern.

**GOVERNANCE_CORE entry**:
```json
"internal": {
  "owner": "Private delta",
  "purpose": "stronger formulations, unresolved tensions, compressed AI notation",
  "scope_prefixes": ["../Internal/"],
  "visibility": "private",
  "entry": "Internal/Framework Control.md",
  "note": "Outside git repo; delta layer only"
}
```

**Fix options**:
- A) Change `scope_prefixes` to `["Internal/"]` (without `../`)
- B) Change `entry` to `../Internal/Framework Control.md` (with `../`)
- C) Accept as exception: internal subsystem intentionally outside repo

**Recommendation**: Option C — document as expected exception. Internal is explicitly "outside git repo; delta layer only."

---

### 3. Subsystem Authoritative Surface Scope  
**Failure**: `subsystem-authoritative-surfaces-in-scope: Some subsystem authoritative surfaces fall outside their declared scope.`

**Root cause**: Same as #2. `root-governance`, `tools`, and `internal` subsystems have authoritative surfaces not matching scope prefixes.

**Affected subsystems**: `root-governance`, `tools`, `internal`

---

### 4. Current Claims Forbidden-Fragment Check
**Failure**: `no-v0-1-governance-active: v0.1 governance files are marked deprecated. Forbidden-fragment check could not run because the file is unavailable.`

**Root cause**: `validate_current_claims()` references v0.1 governance files that are archived.

**Location**: `validate_current_claims()` in `tools/src/research_tools/validate/governance.py`

**Fix required**: Update forbidden-fragment logic to work without v0.1 files.

---

### 5. Current Claims Text Fragments
**Failures**:
- `suf-execution-parallel-instrumentation`: Required fragment missing
- `support-boundary-present`: Required fragment missing

**Root cause**: Text fragment expectations in `SUPPORT_SURFACE_BOUNDARY_EXPECTATIONS` not matching current `research-program.md` content.

**Fix options**:
- A) Update expectations to match current content
- B) Update content to match expectations
- C) Accept drift as legitimate evolution

---

### 6. Edit Scope Baseline
**Failure**: `edit-scope-baseline-readable: Repository edit baseline is missing or empty.`

**Root cause**: v0.1 `REPOSITORY_EDIT_BASELINE_v0_1.json` was archived. No replacement baseline established.

**Expected behavior post-migration**: Baseline intentionally empty — comparison to historical state not meaningful after restructure.

**Fix**: Document as expected state. Baseline comparison skipped when empty.

---

### 7. Edit Scope Changes Within Policy
**Failure**: `edit-scope-changes-within-policy: Some files changed relative to the review baseline fall outside current surfaces, designated work surfaces, and declared scope.`

**Root cause**: Empty baseline means all files appear "changed" vs baseline, triggering policy checks against edit scope.

**Files triggering**: Root Python files (`package_doctor.py`, `authoritative_guard.py`, `research_launch_gate.py`) not in current surfaces.

**Fix required**: Add root-level tooling files to `GOVERNANCE_CORE.edit_scope_policy.always_allowed_files` or `declared_allowed_files`.

---

## Recommended Actions

| Priority | Action | Owner |
|----------|--------|-------|
| P1 | Regenerate `REGISTRY_MANIFEST_v0_2.json` from actual file tree | tooling |
| P1 | Add root Python files to `GOVERNANCE_CORE.edit_scope_policy` | governance |
| P2 | Document internal subsystem scope exception | governance |
| P2 | Update forbidden-fragment check for v0.2-only world | tooling |
| P3 | Reconcile text fragment expectations with current content | content |
| P3 | Accept empty baseline as expected post-migration | governance |

## Validation Command

```bash
cd /home/player/Data/Documents/Obsidian/SUF/Research
tools/.venv/bin/python package_doctor.py
```

Current: 7 failures (down from original 11+).
Target: 0 failures or documented exceptions.

---

**Provenance**: Generated by external-agent during v1.5 tooling validation pass.
**Date**: 2026-04-19
**Status-surface**: Documented debt, not active blocker.
